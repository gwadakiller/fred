backend:
  name: github
  repo: gwadakiller/mon-site
  branch: main

media_folder: static/img
public_folder: /img

collections:
  - name: "docs"
    label: "Documentation"
    folder: "docs"
    create: true
    slug: "{{slug}}"

    fields:
      - { label: "Titre", name: "title", widget: "string" }
      - { label: "Contenu", name: "body", widget: "markdown" }